import { company } from '@/data/site';import type { Locale } from '@/data/content';import { copy } from '@/data/content';import { Clock3, Mail, MapPin, Phone, ArrowUpRight } from 'lucide-react';
export default async function Contact({params}:{params:Promise<{locale:Locale}>}){const {locale}=await params;const t=copy[locale].contact;return <main className="inner-page"><section className="inner-hero contact-hero"><div className="container"><span className="eyebrow">{t.eyebrow}</span><h1 className="display">{t.title}</h1><p>{t.body}</p></div><div className="inner-orb"/></section><section className="contact-v4 section"><div className="container"><div className="contact-cards"><a href={`tel:${company.phone}`} className="contact-tile"><i><Phone/></i><small>{t.phone}</small><h3>{company.displayPhone}</h3><ArrowUpRight/></a><a href={`mailto:${company.email}`} className="contact-tile"><i><Mail/></i><small>{t.email}</small><h3>{company.email}</h3><ArrowUpRight/></a><div className="contact-tile"><i><MapPin/></i><small>{t.location}</small><h3>{company.address}</h3></div><div className="contact-tile"><i><Clock3/></i><small>{t.hours}</small><h3>{company.hours}</h3></div></div><div className="map-stage"><iframe title="Temporary Kothrud map" src="https://www.google.com/maps?q=Kothrud%2C%20Pune%2C%20Maharashtra&output=embed" loading="lazy" referrerPolicy="no-referrer-when-downgrade"/><div className="map-overlay"><span>Temporary map location</span><b>Kothrud · Pune · Maharashtra</b><p>The exact pin can be replaced in one line after client confirmation.</p></div></div></div></section></main>}
